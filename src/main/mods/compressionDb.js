/**
 * Compression Database — DISABLED
 * Kullanıcı isteği doğrultusunda Compression DB özelliği devre dışı bırakıldı.
 * Bu modül boş stub olarak bırakılmıştır; silmek yerine referansların çökmemesi için tutulmuştur.
 */
class CompressionDb {
    async getDb() {
        return [];
    }
    async findEntry() {
        return null;
    }
}

module.exports = new CompressionDb();
